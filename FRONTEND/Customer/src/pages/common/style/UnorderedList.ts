import styled from '@emotion/styled';
import * as colors from 'constants/colors';
export const UlTagStyled = styled(`ul`)({
  paddingLeft: '20px',
});
export const LiTagStyled = styled(`li`)({
  fontSize: '18px',
  fontWeight: 500,
  lineHeight: 1.8,
  color: colors.b9,
});
